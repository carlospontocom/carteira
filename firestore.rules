rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Apenas permite que o usuário autenticado leia e escreva seus próprios dados.
    match /carteiraDigital/{userId}/{documents=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
